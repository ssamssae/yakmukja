# 디자인 리프레시 — 약먹자 + 더치페이 (2026-05-20)

형님 지시(텔레그램, 2026-05-20): 두 앱 색·레이아웃 재설계, 전체 라이트 모드, 디자인 언어 통일 + 앱별 포인트 컬러. 약먹자 = 틸/민트, 더치페이 = 인디고. brainstorm 합의 후 승인("ㅇㅋ", msg 7730).

## 목표 (verifiable)

- 두 앱이 같은 디자인 언어(타이포·간격·컴포넌트 모양)를 공유하되, 포인트 컬러로 정체성 구분.
- 다크 → 라이트 톤 전환. 스플래시 포함 전 화면 밝게.
- 기능 구조(약 리스트·계산기 키패드)는 유지, 비주얼 전면 교체.
- `fvm dart analyze lib/` 0 issue.

## 공통 디자인 시스템

두 앱 각각 `lib/theme/app_theme.dart` 에 동일 구조로 정의(색만 다름).

| 토큰 | 값 |
|------|-----|
| 배경 (scaffold) | `#F7F8FA` 오프화이트 |
| 카드 surface | `#FFFFFF` |
| 카드 그림자 | black 5%, blur 16, offset (0,4) |
| 라운드 | 카드 20 / 버튼 14 / input 12 / 칩 12 |
| 텍스트 강조 | `#1A1D24` |
| 텍스트 본문 | `#4B5563` |
| 텍스트 흐림 | `#9CA3AF` |
| 간격 스케일 | 4 · 8 · 12 · 16 · 20 · 24 |
| 폰트 굵기 | 헤드라인 w800, 타이틀 w700, 본문 w500 |

- 라이트 ThemeData: `Brightness.light`, `useMaterial3: true`, `ColorScheme.fromSeed(seedColor: <포인트>, brightness: light)`.
- AppBar: 투명/흰 배경, `scrolledUnderElevation: 0`, centerTitle.
- 칩/소프트 배경 = 포인트 컬러 alpha 0.12.

## 포인트 컬러

| 앱 | primary | primaryDark(대비용) | soft(배경) |
|----|---------|---------------------|------------|
| 약먹자 | 틸 `#14B8A6` | `#0F9488` | `#14B8A6` @12% |
| 더치페이 | 인디고 `#4F6DF5` | `#3B53D6` | `#4F6DF5` @12% |

- "복용 완료" 상태색은 약먹자 틸 계열로 통일(기존 green 대체 검토 — 틸과 green 충돌 피하려 완료=틸 진한톤, 미복용=중립 회색).

## 약먹자 화면별

- **스플래시** (`splash_screen.dart`): 배경 `#F7F8FA` → 로고 박스(틸 글로우) + "약먹자"(강조 텍스트) + "건강한 하루의 시작"(틸). 다크 → 라이트.
- **홈** (`home_screen.dart`):
  - 상단 진행 카드: 흰 카드 안에 "오늘 n/m 완료" + 진행바(틸). 날짜 보조 텍스트.
  - 다음 복용 카운트다운: 틸 soft 배경 칩.
  - 시간대 그룹(아침/오후/저녁): 그룹 헤더 틸, 약 카드는 흰 카드 + 옅은 그림자.
  - 약 카드: 시간 뱃지(틸 soft) + 이름/용량 + 복용 토글 버튼(틸).
  - FAB: 틸 배경 extended.
  - empty state: 틸 soft 원형 + 아이콘.
- **약 편집** (`medicine_edit_screen.dart`): 라이트 카드/인풋, 저장 버튼 틸. (구조 유지, 톤만)

## 더치페이 화면별

- **스플래시**: 흰 배경 유지 + 로고/포인트 인디고로.
- **메인** (`main.dart`): 금액 표시·인원 조정·결과 카드를 흰 라이트 카드로. 키패드 흰 바탕 + 옅은 구분선. 결과(1인당 금액) 인디고 강조. 계산 버튼 인디고.

## 비범위 (YAGNI)

- 라이트/다크 동시 지원 안 함(라이트 단일).
- 기능 추가·화면 재구성 없음(비주얼 교체만).
- 폰트 패키지 추가 안 함(시스템 폰트 + weight).

## 구현 순서

1. 약먹자: `lib/theme/app_theme.dart` 신설 → main.dart 테마 교체 → splash → home → edit. analyze.
2. 더치페이: 같은 구조 `app_theme.dart` (인디고) → main.dart 전면 교체. analyze.
3. 각 앱 wsl 브랜치 PR. 맥미니 빌드로 실제 렌더 확인 후 조정.

## 검증

- `fvm dart analyze lib/` 0 issue (양 앱).
- 다크 색상 하드코딩(`#121212`,`#1A1A2E`,`#16213E` 등) 잔존 0 (grep).
- 맥미니 빌드 후 형님 시각 확인 = 최종 게이트.
